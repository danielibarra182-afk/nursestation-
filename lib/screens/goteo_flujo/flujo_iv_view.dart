import 'package:flutter/material.dart';

class FlujoIVView extends StatelessWidget {
  const FlujoIVView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Flujo IV View'),
    );
  }
}
